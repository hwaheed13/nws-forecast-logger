# === Node / Vercel projects ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Environment / secrets ===
.env
.env.local
.env.*.local

# === Build outputs ===
.next/
out/
dist/
build/
.vercel/

# === Logs ===
*.log
logs/
*.csv
*.sqlite

# === OS cruft ===
.DS_Store
Thumbs.db

# === IDE cruft ===
.vscode/
.idea/
sync.log
scripts/*.log
bucket_model.pkl
model_metadata.json
temp_model.pkl
nws-forecast-logger/
.venv/

# === Python cruft (audit 2026-07-25) ===
__pycache__/
*.pyc
*.pyo

# The global *.csv rule above predates the tracked data CSVs (they are
# force-tracked). The production predictions ledger MUST be committable by
# the nightly workflow, so exempt it explicitly:
!*predictions_ledger.csv

# === Local/session artifacts — never commit ===
.claude/worktrees/
api.py.save
*.save
*.bak-*
*.snapshot
dsm_capture_log_*.txt
dsm_run_report.md
scheduled_task_runs/
atm_snapshot_cache.json
temp.csv
